checkList ( yes / no )

1. 나는 spa와 csr을 이해하고 a태그나 location 객체를 사용하면 안되는 이유를 알고 있다.
    spa : 페이지가 하나인 웹

    csr: client side rendering :사용자 화면에서 띄운다

    a태그나 location객체를 사용하면 안되는 이유 : 
        페이지를 새로 불러와서 이미 갖고왔던 상테가 초기화되고 랜더링된 컴포넌트도 모두 사라져 새로 랜더링을 해야된다.
        a 태그의 href는 페이지를 이동시킬 때 페이지를 새로 불러오기 때문에 상태 값이 유지되지 못하고 속도도 저하된다.
        a 태그 대신 => link태그
        location 대신 => (use)Navigate

2. 나는 프론트엔드에서 주소로 데이터를 관리하는 이유를 알고 있다.
    알고있다!
    주소에 데이터값을 저장하여 이전데이터로 돌아갈때.........뒤로가기..

3. 나는 react에서 주소로 데이터를 관리하고 올바르게 참조(불러올 수)할 수 있다.
 
4. 나는 컴포넌트를 나누는 기준에 대해서 알고 있다.



1. 나는 styled-components를 활용하여 js 안에 css 문법을 작성할 수 있는가

2. 나는 styled-components로 생성된 컴포넌트에 값을 전달하고 값에 따라 다른 css를 줄 수 있는가

2. 나는 부모 컴포넌트와 자식 컴포넌트를 구분하고 부모컴포넌트의 값을 자식 컴포넌트에게 전달할 수 있는가

3. 나는 구조분해할당의 개념을 이해하고 객체와 배열을 구조분해할 수 있는가, 또한 리엑트에는 이러한 구조분해할당이 어느 곳에서 이루어지고 있는가

4. 나는 공용으로 사용해야하는 컴포넌트를 구분하고 전달된 값들을 활용하여 다양한 형태의 UI를 만들 수 있는가


5. 공용 컴포넌트로 전달되는 값은 재사용되는 UI에 따라 크게 바뀐다. 이를 유추하고 구현할 수 있는가